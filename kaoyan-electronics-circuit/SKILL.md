---
name: kaoyan-electronics-circuit
description: 822电子技术基础电路图与电路题分析模块。用于图片/文字电路识别、模电静态/动态分析、数电逻辑/时序分析、电路图生成、微变等效图生成，并自动衔接题型SOP和错题归因。
---

# 822电路题分析

## 角色定位

本模块只处理“看图、识图、建模、分析”。目标不是泛泛解释电路，而是把电路题转化为 822 可作答的结构。

生成电路图时，必须先读取 `references/circuit-diagram-generation.md`，默认输出 Obsidian Excalidraw `.md`；用户明确要求教材级图时再用 `circuitikz` 或 SVG。

---

## 输入类型

| 输入 | 处理方式 |
| --- | --- |
| 模电电路图 | 识别器件与偏置，先静态后动态 |
| 数电逻辑图 | 写表达式、真值表或状态关系 |
| 时序电路图 | 写驱动方程、状态方程、状态表 |
| 文字描述电路 | 先重构拓扑，再选择SOP |
| 生成电路图 | 先写结构化网表，再生成可编辑图 |
| 生成微变等效图 | 先给原图，再给等效模型和观察端 |

---

## 模电分析模板

1. 电路类型识别。
2. 工作区判断。
3. 直流通路与静态工作点：$I_{BQ}$、$I_{CQ}$、$U_{CEQ}$。
4. 交流微变等效。
5. 动态指标：$A_u$、$R_i$、$R_o$。
6. 易错点：负载、电容开短路、反馈、符号方向。

---

## 数电分析模板

1. 电路类型识别。
2. 逻辑表达式或驱动方程。
3. 真值表、状态表或状态图。
4. 功能描述。
5. 自启动/竞争冒险/无效状态检查。
6. 对应SOP和错题标签。

---

## 输出要求

- 先给“电路识别结论”，再展开计算。
- 看不清的元件参数必须显式标注不确定，不硬猜。
- 上传图片时，优先提取题干文字和元件数值。
- 需要做完整题解时，转入 `kaoyan-electronics-sop` 的对应流程。
- 生成图示时，必须检查输入输出、接地、电源、器件方向、反馈路径和悬空节点。

---

## 电路图生成模式

当用户说“画一个电路图”“生成共射放大图”“画微变等效电路”等，执行：

1. 读取 `references/circuit-diagram-generation.md`。
2. 生成结构化网表。
3. 选择模板：共射、运放、反馈、差分、功放、稳压、逻辑门或时序电路。
4. 默认生成 Obsidian Excalidraw 文件，保存到对应课程附件目录。
5. 同时给出检查清单，说明图中每个关键节点的学习作用。

---

## 常见错因标签

| 标签 | 场景 |
| --- | --- |
| circuit | 拓扑、信号流、反馈路径看错 |
| formula | 增益、状态方程、功率公式写错 |
| state | 状态转换或自启动漏检 |
| condition | 虚短虚断、工作区、饱和/截止条件误用 |
| calculation | 代数和数值计算错误 |

---

## 引用规范

- 电路图生成规范：`references/circuit-diagram-generation.md`
- 知识库说明：[[考研专业课/📐 822电路图绘制规范]]
